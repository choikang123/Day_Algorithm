이 문제 딕셔너리를 쓰려고 했는데 딕셔너리를 쓰면 안되는 이유가 있었음
딕셔너리를 쓰면 값을 덮어쓰기 때문에 

그러면 index:value로 하면 index는 안겹치니까 가능한거 아니야?라고 생각할 수 있지만
이렇게 되면 정렬은 되지만 맨앞에걸 뽑아서 뒤로 보내는 행위가 딕셔너리에서는
딕셔너리는 keys() 순회를 해야 하므로 사실상 O(n) 연산과 같다.
리스트나 deque를 쓰면 앞에서 꺼내서 뒤에 넣는 행위가 O(1)로 빠름

그리고 모두를 만족해야 하는 조건에서 
즉 일단 내가(큐에서 pop해서 꺼낸 값이) 정답이야 짱이야라고 설정해둬야
남이 없어도 즉 pop해서 꺼낸후 큐가 비어있어도 
